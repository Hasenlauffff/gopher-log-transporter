kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: gopher-cluster
nodes:
  - role: control-plane
  - role: worker
    extraMounts:
      - hostPath: /var/log/pods
        containerPath: /var/log/pods
 